{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Nest/nestjs-mvc/package.json",
                "localRoot": "${workspaceFolder}/Nest/nestjs-mvc"
            }
        }
    ]
}